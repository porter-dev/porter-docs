organization: Porter
apis:
  porter@v1:
    root: ./docs/api/api.yaml
    lint:
      rules:
        no-ambiguous-paths: error
lint:
  extends:
    - recommended
features.openapi:
  schemaExpansionLevel: 2
  generateCodeSamples:
    languages:
      - lang: curl
      - lang: Python
