To deploy from a public registry, you can input the name of a public Docker image. For images from Docker Hub, you can simply specify their name without a URI, for example:

TODO

For other registries, input the image repository domain and path. For example, to deploy from the public ECR Ubuntu image, you can deploy the following:

TODO

Select "Continue" after specifying your image.
